# Wheel of Pasiva - Screen Navigation Diagram

## Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              App.kt (Entry Point)                           │
│                         SimpleNavigator manages state                       │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │   Screen.Home (init)  │
                          └───────────┬───────────┘
                                      │
              ┌───────────────────────┴───────────────────────┐
              ▼                                               ▼
    ┌─────────────────┐                           ┌─────────────────────┐
    │   Screen.Home   │◄─────────────────────────►│  Screen.Room(id)    │
    └─────────────────┘    navigateToRoom(id)     └─────────────────────┘
                           navigateBack()
```

---

## Screen Details

### 1. HomeScreen
**File:** `presentation/home/HomeScreen.kt`
**ViewModel:** `HomeViewModel`
**Route:** `Screen.Home`

```
┌─────────────────────────────────────────────────────────┐
│                    HomeScreen                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │              TopAppBar                            │  │
│  │            "Wheel of Pasiva"                      │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│                      "Welcome!"                         │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  TextField: "Enter your nickname"                 │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Button: "Create Room"                            │──┼──► Creates room → navigateToRoom(roomId)
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Button: "Join Room"                              │──┼──► Opens JoinRoomDialog
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  [Error message displayed when HomeUiState.Error]       │
└─────────────────────────────────────────────────────────┘
```

**States (HomeUiState):**
- `Idle` - Initial state
- `Loading` - Creating room in progress
- `Error(message)` - Room creation failed

**Navigation Events:**
- `NavigateToRoom(roomId)` - After successful room creation
- `ShowJoinDialog` - Opens join room dialog

---

### 2. JoinRoomDialog (Modal)
**File:** `presentation/home/JoinRoomDialog.kt`
**ViewModel:** `JoinRoomViewModel`
**Type:** AlertDialog (overlay on HomeScreen)

```
┌─────────────────────────────────────────────────────────┐
│                   JoinRoomDialog                        │
│                                                         │
│                    "Join Room"                          │
│                                                         │
│  "Enter the 6-digit room code or scan a QR code"       │
│                                                         │
│  ┌─────────────────────────────────────────┬─────────┐  │
│  │  TextField: "Room Code"                 │   QR    │  │
│  │  placeholder: "123456"                  │  Icon   │──┼──► Opens QR Scanner (TODO)
│  └─────────────────────────────────────────┴─────────┘  │
│                                                         │
│  [Error message when JoinRoomUiState.Error]             │
│  [Loading spinner when JoinRoomUiState.Loading]         │
│                                                         │
│  ┌───────────────┐              ┌───────────────────┐   │
│  │    Cancel     │              │       Join        │───┼──► Validates → navigateToRoom(roomId)
│  └───────────────┘              └───────────────────┘   │
│        │                                                │
│        └──► Dismisses dialog                            │
└─────────────────────────────────────────────────────────┘
```

**States (JoinRoomUiState):**
- `Idle` - Initial state
- `Loading` - Joining room in progress
- `Error(message)` - Room not found or join failed

**Navigation Events:**
- `NavigateToRoom(roomId)` - After successful join
- `OpenQrScanner` - Opens camera for QR scanning (not yet implemented)

---

### 3. RoomScreen
**File:** `presentation/room/RoomScreen.kt`
**ViewModel:** `RoomViewModel`
**Route:** `Screen.Room(roomId: String)`

```
┌─────────────────────────────────────────────────────────┐
│                     RoomScreen                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │  ← Back    TopAppBar: "Room {roomId}"             │  │
│  └──────┬────────────────────────────────────────────┘  │
│         │                                               │
│         └──► navigateBack() → HomeScreen                │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │                    Card                           │  │
│  │              "Room Code"                          │  │
│  │               {ROOM_ID}                           │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│              "Players ({count})"                        │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  LazyColumn: Player Cards                         │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  👤  {nickname}  [HOST]  "Joined at HH:MM"  │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  👤  {nickname}          "Joined at HH:MM"  │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │                    ...                            │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  [Shows "No players yet" when list is empty]            │
│  [Shows loading spinner when RoomUiState.Loading]       │
│  [Shows error + Retry when RoomUiState.Error]           │
└─────────────────────────────────────────────────────────┘
```

**States (RoomUiState):**
- `Loading` - Fetching room data
- `Success(room, players)` - Room data loaded, real-time updates
- `Error(message)` - Failed to load room

**Features:**
- Real-time player list updates via Firestore snapshots
- Host badge displayed for room creator
- Retry button on error state

---

## Navigation Flow Diagram

```
                                    APP START
                                        │
                                        ▼
                              ┌─────────────────┐
                              │   HomeScreen    │
                              │  Screen.Home    │
                              └────────┬────────┘
                                       │
                   ┌───────────────────┼───────────────────┐
                   │                   │                   │
                   ▼                   ▼                   │
          "Create Room"         "Join Room"               │
                   │                   │                   │
                   ▼                   ▼                   │
        ┌──────────────────┐  ┌─────────────────┐         │
        │ HomeViewModel    │  │ JoinRoomDialog  │         │
        │ createRoom()     │  │ (Modal Overlay) │         │
        └────────┬─────────┘  └────────┬────────┘         │
                 │                     │                   │
                 │            ┌────────┴────────┐         │
                 │            │                 │         │
                 │      Enter Room ID    Scan QR (TODO)   │
                 │            │                           │
                 │            ▼                           │
                 │   JoinRoomViewModel.joinRoom()         │
                 │            │                           │
                 ▼            ▼                           │
        ┌─────────────────────────────────┐               │
        │     NavigationEvent.            │               │
        │     NavigateToRoom(roomId)      │               │
        └────────────────┬────────────────┘               │
                         │                                │
                         ▼                                │
               ┌─────────────────┐                        │
               │   RoomScreen    │                        │
               │ Screen.Room(id) │                        │
               └────────┬────────┘                        │
                        │                                 │
                        │    Back Button                  │
                        └─────────────────────────────────┘
```

---

## Component Relationships

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              AppContainer (DI)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  provideHomeViewModel()     → HomeViewModel                                 │
│  provideJoinRoomViewModel() → JoinRoomViewModel                             │
│  provideRoomViewModel(id)   → RoomViewModel(roomId)                         │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ provides
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            ViewModels                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│  HomeViewModel                                                              │
│   ├── uiState: StateFlow<HomeUiState>                                       │
│   ├── nickname: StateFlow<String>                                           │
│   ├── navigationEvent: SharedFlow<NavigationEvent>                          │
│   ├── onCreateRoom()                                                        │
│   ├── onJoinRoom() → emits ShowJoinDialog                                   │
│   └── onNicknameChange(name)                                                │
│                                                                             │
│  JoinRoomViewModel                                                          │
│   ├── uiState: StateFlow<JoinRoomUiState>                                   │
│   ├── roomId: StateFlow<String>                                             │
│   ├── navigationEvent: SharedFlow<NavigationEvent>                          │
│   ├── onJoinRoom(nickname)                                                  │
│   ├── onRoomIdChange(id)                                                    │
│   ├── onOpenQrScanner()                                                     │
│   └── reset()                                                               │
│                                                                             │
│  RoomViewModel(roomId)                                                      │
│   ├── uiState: StateFlow<RoomUiState>                                       │
│   ├── getRoomId(): String                                                   │
│   ├── isHost(player, room): Boolean                                         │
│   └── retry()                                                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## File Structure

```
presentation/
├── navigation/
│   └── SimpleNavigation.kt      # Screen sealed class + SimpleNavigator
│
├── home/
│   ├── HomeScreen.kt            # Main home screen composable
│   ├── HomeViewModel.kt         # Create room logic
│   ├── HomeUiState.kt           # Idle/Loading/Error states
│   ├── JoinRoomDialog.kt        # Modal dialog for joining
│   ├── JoinRoomViewModel.kt     # Join room logic
│   └── JoinRoomUiState.kt       # Idle/Loading/Error states
│
└── room/
    ├── RoomScreen.kt            # Room display with player list
    ├── RoomViewModel.kt         # Room observation logic
    └── RoomUiState.kt           # Loading/Success/Error states
```

---

## Future Screens (Planned)

```
┌───────────────────┐     ┌───────────────────┐     ┌───────────────────┐
│   QrScannerScreen │     │   WheelScreen     │     │   ResultScreen    │
│   (Camera View)   │     │   (Spin Wheel)    │     │   (Task Result)   │
└───────────────────┘     └───────────────────┘     └───────────────────┘
        │                         │                         │
        └─────────────────────────┴─────────────────────────┘
                                  │
                    (Not yet implemented)
```
